on:
  workflow_dispatch:
jobs:
  job1:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Setup Node
      uses: actions/setup-node@v1
      with:
        node-version: '18.x'
    - name: Install dependencies
      run: ls
    - name: lint
      run: npm run lint:frontend
           npm run eslint
    - name: Test 
      run: npm run test
    
